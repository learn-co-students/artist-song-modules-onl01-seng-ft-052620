module Memorable

        def reset_all
            self.all.clear
        end

        def count
            self.all.count
        end
end

# Inside here, define your `reset_all` and `count` methods.